# Retail Shop Dashboard

This is the revised Streamlit dashboard for visualizing retail shop data.

## Files
- `revised_shop_dashboard.py` — the Streamlit app
- `revised_shop.csv` — input data (ensure this file is in the repo root)
- `requirements.txt` — Python dependencies

## Run locally
```bash
pip install -r requirements.txt
streamlit run retail_dashboard.py
